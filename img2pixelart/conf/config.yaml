# 全局 settings —— 由 hydra-core 加载（img2pixelart/cli.py -> load_settings）
defaults:
  - _self_
  - perceive: default
  - structure: default
  - render: default

# 全局开关，可用命令行覆盖，如 --setting auto_background=true
auto_background: false
